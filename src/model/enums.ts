export namespace enums 
{
    export enum configurations {
        DEVELOPMENT = 'development',
        RELEASE = 'release'
    };

    export enum players {
        PLAYER1 = 'player1',
        PLAYER2 = 'player2'
    };
};